<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
	xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
	<modelVersion>4.0.0</modelVersion>
	<parent>
		<groupId>org.postgresql</groupId>
		<artifactId>pljava.app</artifactId>
		<version>1.6.0-SNAPSHOT</version>
	</parent>
	<artifactId>pljava</artifactId>
	<name>PL/Java backend Java code</name>
	<description>Java stored procedure implementation for PostgreSQL</description>
	<dependencies>
		<dependency>
			<groupId>org.postgresql</groupId>
			<artifactId>pljava-api</artifactId>
			<version>${project.version}</version>
		</dependency>
	</dependencies>

	<build>
		<plugins>
			<plugin>
				<groupId>org.apache.maven.plugins</groupId>
				<artifactId>maven-compiler-plugin</artifactId>
				<configuration>
					<compilerArgs>
						<arg>-h</arg>
						<arg>${basedir}/target/javah-include</arg>

						<arg>--processor-module-path</arg>
						<arg>${basedir}/../pljava-api/target/pljava-api-${project.version}.jar</arg>
					</compilerArgs>
				</configuration>
			</plugin>
		</plugins>
	</build>
	<reporting>
		<plugins>
			<plugin>
				<groupId>org.postgresql</groupId>
				<artifactId>pljava-pgxs</artifactId>
				<version>0.0.1-SNAPSHOT</version>
				<reportSets>
					<reportSet>
						<reports>
							<report>
								scripting-report
							</report>
						</reports>
					</reportSet>
				</reportSets>
				<configuration>
					<script mimetype="application/javascript">
						var ArrayList = Java.type("java.util.ArrayList");
						var args = new ArrayList();
						args.add("-doctitle");
						args.add("'${project.name} ${project.version}'");
						args.add("-windowtitle");
						args.add("'${project.name} ${project.version}'");
						args.add("--module-path");
						args.add("${user.home}/org/postgresql/pljava-api/1.6.0-SNAPSHOT/pljava-api-1.6.0-SNAPSHOT.jar");
						args.add("--module-source-path");
						args.add("${basedir}/target/site/apidocs/src");
						args.add("--module");
						args.add("org.postgresql.pljava.internal");
						args.add("--module-source-path");
						args.add("org.postgresql.pljava.internal=${basedir}/src/main/java");
						args.add("--show-module-contents");
						args.add("all");
						args.add("--show-packages");
						args.add("all");
						args.add("-link");
						args.add("'https://docs.oracle.com/javase/10/docs/api'");
						args.add("-d");
						args.add("${basedir}/target/site/apidocs");
						args.add("-protected");
						args.add("${basedir}/src/main/java/module-info.java");

						var tool = javax.tools.ToolProvider.getSystemDocumentationTool();
						var task = tool.getTask(null, null, null, null, args, null);
						task.call();
					</script>
				</configuration>
			</plugin>
		</plugins>
	</reporting>
</project>
